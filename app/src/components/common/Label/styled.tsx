import styled from 'styled-components';

const TextColor = '#3f4656';

export const LabelStyle = styled.label`
    display: block;
    margin-bottom: 4px;
    font-size: 14px;
    color: ${TextColor};
    font-weight: 500;
`;